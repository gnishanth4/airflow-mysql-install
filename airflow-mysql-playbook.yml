---
 - name: Install MYSQl and Airflow in ubuntu
   hosts: localhost
   connection: local
   become: yes

   roles:
        - role: pre-packages
        
        - role: mysql

        - role: airflow  

 

